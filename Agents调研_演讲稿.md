# Agents 调研｜逐页演讲稿

面向内部产品与技术团队，建议时长 20–25 分钟。各页讲稿已同步写入 PPT 备注。

## 01 Agents 调研

今天围绕一个问题展开：一个 Agent 从开发者手里的代码或配置，怎样成为别人能发现、能复用、能在业务中使用的能力？我们先统一资产与消费的概念，再看 Space、AgentHub 和企业工作台三类产品，最后讨论我们自己的建设顺序。整场分享重点放在产品职责和能力衔接。

## 02 本次分享的逻辑

我们按照从可见体验到资产管理、再到业务消费的顺序观察产品。先用两个维度建立共同语言，随后用实际截图解释每类平台解决什么问题。最后回到完整生命周期，讨论哪些能力应该共用、哪些职责需要分开。这是本次分享的章节顺序，并不意味着所有 Agent 都必须依次经过这些平台。

## 03 Agent 的资产形态

第一步是明确我们到底要管理什么。图中把主要资产载体分成代码工程、工作流或 DSL、声明式定义，以及整包或分发包。它们可以组合，并不互斥。Skill、工具、MCP 服务、Prompt 和知识资源是组成资产，不能直接全部等同于完整 Agent。实例状态、镜像和服务描述则属于运行与交付相关资产。后续讨论资产中心时，需要明确收什么，以及版本化的边界。

## 04 Agent 的消费与集成形态

资产是什么，与怎么使用它，是两个不同维度。同一个 Agent 可以通过 SDK 或 API 接入，也可能作为子代理参与编排，最后通过 Web、IDE 或企业工作台触达用户。触发方式还可以是交互、事件或定时任务。这里不要把某个协议当成整个产品形态。平台设计需要分别回答接入方式、参与角色、使用入口，以及何时启动和如何返回结果。

## 05 三类平台在生命周期中的位置

这张全景图把后面的案例放在一起。Space 的核心是可运行、可体验的应用。AgentHub 的核心是可管理和可复用的资产。企业工作台则关心员工如何找到并使用这些能力。图中的连线表达一种生态衔接思路，不能把它直接理解为这些产品已经彼此打通。下面先从最直观的 Space 看起，观察一个项目如何变成用户可以打开的应用。

## 06 Space 的产品定位

Space 解决的是应用化问题。开发者把代码和依赖放到平台，配置运行环境，再得到一个可访问的 Web 应用。材料对比了 Hugging Face Spaces 与 ModelScope 创空间，共同关注仓库、应用框架、构建部署和算力资源。这里要保留一个边界：Space 可以承载各种 AI 应用，并不是每一个 Space 都必然是 Agent。

## 07 应用广场是体验入口

这是 ModelScope 创空间的应用广场。对于普通访问者，第一步不是理解代码或框架，而是找到一个感兴趣的场景并尝试。应用卡片承担发现和分发作用。对平台而言，演示的质量、访问是否顺畅以及应用能否持续运行，会直接影响用户体验。接下来从开发者视角看，一个应用如何进入这样的广场。

## 08 创建空间：提交应用文件

创建过程先要求上传空间文件或文件夹。这里上传的是能够被构建和运行的工程内容，而不仅是一段对 Agent 的自然语言描述。大家可以看页面顶部的三步流程：空间文件、部署设置、确认并部署。后面两页分别对应运行环境配置和部署前核对，体现 Space 以应用部署为中心的产品路径。

## 09 部署设置：确定应用如何运行

第二步是部署设置。截图里可以看到 Gradio、Streamlit、Static 和 Docker 等选项，以及计算资源和镜像等配置。这些信息说明，Space 除了提供展示页面，还需要完成应用运行的基础工作。我们在规划类似能力时，应把应用部署体验与底层运行平台的职责衔接好。截图里的资源规格只代表当时的界面，不在这里做价格或配额比较。

## 10 确认部署：核对文件和运行配置

第三步把文件列表与部署设置汇总在一个页面，帮助开发者核对发布内容。可以看到，整个流程围绕工程输入、环境选择和部署确认展开。它与单纯上传一个资产包有明显区别，因为后续还要承担构建和运行。对我们的产品设计而言，这一页提醒我们把资产上传成功和应用部署成功区分开，并为不同阶段提供清晰的状态反馈。

## 11 运行后的应用：WindowSeat 示例

部署完成后，用户进入的是具体应用界面。WindowSeat 示例提供上传图片和示例素材，访问者能够直接围绕任务操作。这里的观察重点是，平台承载的是应用体验，而应用内部可以有自己的交互设计。评价这类产品时，需要关注打开速度、操作反馈、错误提示和应用是否可用，而不能只看应用广场上有多少个项目。

## 12 Hugging Face Spaces：同类应用化路径

这是 Hugging Face Spaces 中的 Qwen 图像应用页面。它同样把能力包装成输入区、参数和结果区，让用户可以直接尝试。两组截图帮助我们确认 Space 的共性：把工程和运行环境组织成可访问的应用。但一个应用跑起来以后，怎样保存版本、管理组成能力和支持其他人复用？这就引出下一章的 AgentHub。

## 13 AgentHub 的产品定位

AgentHub 的重点转向资产管理。调研图围绕仓库、版本、Workspace、CLI 和社区分享组织能力。这里的 Workspace 指 Agent 的文件、配置和相关资源组织，并不是后面面向员工的企业工作台。图中也列出了跨框架转换、同步和备份等能力方向，具体适用框架和转换范围仍需要逐项验证。接下来用实际页面看哪些信息直接呈现在用户面前。

## 14 资产发现：按场景和框架找到 Agent

AgentHub 首页看起来也是一个广场，但卡片上的信息更偏向 Agent 资产本身，例如所属框架以及 Skill、MCP 等组成信息。这和 Space 面向可运行应用的展示重点不同。对于寻找可复用能力的开发者，除了知道它能做什么，还需要判断它适不适合当前技术栈，拿到后需要哪些依赖，以及应该怎样安装或使用。

## 15 资产详情：描述、声明与安装入口

进入一个 Agent 详情页后，可以看到名称、描述、框架、标签、许可证等信息，右侧还有下载和安装入口。这些字段构成用户判断是否复用的基础。对资产标准而言，描述性元数据只是第一层，还需要关联具体文件、依赖和版本。这也解释了为什么资产中心需要统一声明格式，而不仅是给压缩包增加一个名称。

## 16 组成能力：Skill 与 MCP 依赖

这一页把 Agent 使用的 Skill 和 MCP 信息单独展示出来。这样用户在下载之前就能了解它需要什么能力。对于我们的资产管理方案，依赖应该能够被识别、关联和版本化。还要区分依赖的声明与实际可用性：能够列出一个 MCP 服务，并不代表当前环境已经完成授权或具备访问条件。依赖检查应当贯穿安装和运行过程。

## 17 资产文件：定义和配置需要可检查

文件页展示了 AGENTS.md、README.md、mcp.json、skills.json 和技能目录等内容。这些文件帮助我们理解一个 Agent 到底交付了什么。仓库视图也给后续版本比较、问题定位和迁移提供基础。我们可以借鉴的是“声明、依赖、实际文件能够对应”的设计原则，具体文件结构则需要根据自己的框架和资产标准确定，不能要求所有框架完全一致。

## 18 发布入口：界面创建与 CLI 上传

新建入口同时提供自定义创建和通过 CLI 上传两条路径。可视化方式适合配置型资产，本地工程则更适合命令行上传和自动化集成。产品上可以复用同一份资产声明和校验规则，避免两条发布路径产生不同标准。这里的观察不在于入口有多少，而在于它们能否把不同来源的资产纳入同一套管理机制。

## 19 创建 Agent：组合框架、模型与工具

创建页面把框架、系统设定、模型、Skill 和 MCP 等组成部分展开。这说明平台不只是托管任意文件，也试图理解 Agent 的结构。到这里可以总结 AgentHub 的价值：让人看懂、拿到并复用 Agent。接下来的问题是，企业员工通常不会从下载文件开始工作，他们需要在任务入口中直接使用能力。这就进入企业工作台这一层。

## 20 企业工作台的能力范围

调研材料以 Gemini Enterprise 为例，归纳了企业工作台的能力范围，包括搜索、默认助手、Agent Gallery、工作流、技能、连接器和治理。理解这一页时，重点放在企业使用视角：员工如何发现能力，能力如何接触企业数据，以及管理员如何控制访问。图中概览包含调研归纳，后面只围绕截图能够观察到的入口与操作展开，不把全景图当作逐项验证的功能清单。

## 21 统一入口：在对话中选择能力

工作台首页以对话作为主要入口，在输入过程中可以选择智能体和技能。它把多种能力放在用户正在处理任务的位置，减少切换成本。这与 AgentHub 的仓库浏览不同，用户的目标是完成工作，而不是管理资产包。产品设计需要同时考虑默认助手与专用 Agent 如何协作，以及用户如何理解当前由哪个能力处理任务。

## 22 技能管理：沉淀可复用的工作规则

截图展示了 brand-voice 技能，内容是品牌表达和措辞规则。这个例子说明，企业知识不仅是文档检索对象，也可以沉淀成执行时使用的规则。工作台需要提供创建、导入和维护这些规则的方式。与此同时，资产侧的 Skill 定义和消费侧的使用入口要能够对应，否则同一份能力容易在不同产品中重复配置、难以更新。

## 23 Agent Gallery：发现官方与个人智能体

智能体页面按来源组织可用 Agent，截图中可以看到官方能力和用户自己的智能体。原图还标注了 Gallery 与 Marketplace 关联需要人工开通的调研观察。这提示我们，目录里可发现、组织已获授权、当前用户能使用，是不同的状态。设计企业能力目录时，需要把这些状态表达清楚，不能简单把公共资产列表原样搬进企业环境。

## 24 创建智能体：自然语言与手动配置

创建入口允许用户描述想要的智能体，也提供手动配置路径。与前面 Space 的上传工程相比，这里更接近业务用户的表达方式。两条路径最终都需要形成可管理的定义，并且明确能访问哪些数据、调用哪些工具。对于企业产品，降低创建门槛只是开始，还要让创建者能够验证效果、发布给合适的人，并持续维护。

## 25 连接器：接入企业数据与系统

连接器页面列出了不同企业服务和工具的接入项。它们决定了 Agent 是否能够读取相关信息或执行后续操作。可以看到，企业工作台并不只是把聊天窗口和 Agent 列表放在一起，更需要处理账号、授权和数据范围。规划时应把连接器与权限治理一起考虑，并在用户执行任务时清楚地反馈哪些连接已就绪，哪些还需要配置。

## 26 Marketplace：引入外部 Agent 能力

这是调研材料里的 Google Cloud Marketplace 页面，展示了面向 Gemini Enterprise 的相关条目。市场承担外部能力发现与商业分发，工作台承担企业内的实际使用，两者不能混为同一个产品职责。对于我们的生态规划，也需要明确公共目录、企业目录和商业市场之间的关系，避免用户找到能力后还不知道怎样把它接入自己的组织。

## 27 三类平台的职责对照

看完案例后，可以把职责边界浓缩到这张表。Space 管应用的运行和体验，AgentHub 管资产的描述、版本和复用，企业工作台管员工任务和企业系统连接。它们可以共享身份、元数据和权限基础，但不必合并成一个产品。这里的判断是基于材料的产品归纳，用来指导我们接下来的职责划分，而不是给不同产品做功能排名。

## 28 生态建设方案：先明确职责与衔接

最后这张图是调研材料提出的华为云生态建设方案，需要作为规划建议来看。它把开源开发、资产中心、运行平台、Space、企业工作台和云商店放在完整生命周期中。关键不是同时把每一块都做大，而是先回答资产的权威来源在哪里、产品各自负责什么，以及资产如何进入运行环境。具体产品分工和路线图都需要进一步评审确认。

## 29 建设优先级与本次讨论事项

材料给出了三个建议阶段。P0 先统一资产标准和 Registry，明确身份、版本、依赖及产品边界。P1 再建设上传下载、搜索和 CLI，并打通资产与运行平台，形成可演示的闭环。P2 才逐步扩展企业工作台和商业分发，完善治理与生态。图中的月份是建议窗口，不是已确认承诺。本次讨论建议聚焦三项产出：确定统一资产定义和权威来源，确认各产品负责人及边界，选定一个贯穿开发、发布、运行和企业使用的验证场景。

